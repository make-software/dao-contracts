[workspace]
members = [
    "dao-utils",
    "dao-macros/sample-contract",
    "dao-macros",
    "dao-modules",
    "dao-contracts",
    "dao-erc20",
    "dao-erc721",
]

[profile.release]
codegen-units = 1
lto = true

[profile.dev.package."*"]
opt-level = 3
