use casper_dao_utils::TestEnv;
use casper_types::U256;
use erc20::ERC20Test;

static NAME: &str = "Plascoin";
static SYMBOL: &str = "PLS";
static DECIMALS: u8 = 2;

// fn setup(initial_supply: U256) -> ERC20Test {
//     // let token = ERC20Test::new(&TestEnv::new());
// }

#[test]
fn test_erc20() {
    assert_eq!(1, 2);
}

fn main() {}